diff --git a/objects/obj_game/Create_0.gml b/objects/obj_game/Create_0.gml
index d02a907..0b9c52b 100644
--- a/objects/obj_game/Create_0.gml
+++ b/objects/obj_game/Create_0.gml
@@ -22,6 +22,11 @@ window_set_size(SCREEN_W * SCREEN_SCALE, SCREEN_H * SCREEN_SCALE);
 surface_resize(application_surface, SCREEN_W, SCREEN_H);
 display_set_gui_size(SCREEN_W, SCREEN_H);
 gpu_set_texfilter(false);
+// Serfs only move on a game tick, and TICK_LENGTH_MS is 20 (50 ticks/sec).
+// Drawing at 60 fps gives five drawn frames per four ticks, so one frame in
+// five repeats the previous position - the stutter reads as "moves a bit, then
+// snaps". Running the room at 50 puts ticks and frames 1:1.
+game_set_speed(TICKS_PER_SEC, gamespeed_fps);
 camera_set_view_size(view_camera[0], SCREEN_W, SCREEN_H);
 alarm[0] = 1;   // centre window next frame (size change must settle first)
 
