diff --git a/scripts/scr_viewport/scr_viewport.gml b/scripts/scr_viewport/scr_viewport.gml
index 435507a..07e0a6f 100644
--- a/scripts/scr_viewport/scr_viewport.gml
+++ b/scripts/scr_viewport/scr_viewport.gml
@@ -1021,10 +1021,11 @@ function Viewport(_interface, _map) : GuiObject() constructor {
 
         if (_type <= Terrain.water3) {
             _sprite = 9;
-        } else if (_type >= Terrain.snow0) {
-            _sprite += 6;
         } else if (_type >= Terrain.desert0) {
-            _sprite += 3;
+            /* Grey set for rock and snow alike. Sprites 3-5 are baked from the
+               brown ground tiles, so a path over a mountain came out the same
+               colour as the rock under it; 6-8 are grey and read clearly. */
+            _sprite += 6;
         }
 
         // Frame::draw_masked_sprite(AssetPathMask, mask, AssetPathGround, sprite):
@@ -1089,10 +1090,9 @@ function Viewport(_interface, _map) : GuiObject() constructor {
 
         if (_type <= Terrain.water3) {
             _sprite = 9; /* Bouy */
-        } else if (_type >= Terrain.snow0) {
-            _sprite += 6;
         } else if (_type >= Terrain.desert0) {
-            _sprite += 3;
+            /* Matches draw_path_segment above: grey for rock and snow. */
+            _sprite += 6;
         }
 
         gfx_draw_sprite_off(_lx, _ly, Asset.map_border, _sprite, false);
